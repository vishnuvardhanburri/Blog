const nextConfig = {
  output: 'export',
  
  // basePath: '/portfolio',
  
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
  
  trailingSlash: true,
}

export default nextConfig
